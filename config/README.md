# Project configuration

Contains Django settings and deployment entry points (`urls.py`, `asgi.py`, and `wsgi.py`). Business rules do not belong here; settings should configure applications and infrastructure only.
